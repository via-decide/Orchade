import fs from 'node:fs';
import path from 'node:path';
const root = process.cwd();
const manifest = JSON.parse(fs.readFileSync(path.join(root, 'gameplay/module-manifest.json'), 'utf8'));
const modules = manifest.modules;
const ensure = p => fs.mkdirSync(path.join(root, p), {recursive:true});
const write = (p, s) => { ensure(path.dirname(p)); fs.writeFileSync(path.join(root,p), s); };
const title = s => s[0].toUpperCase()+s.slice(1);
for (const m of modules) {
 const dir=`gameplay/${m.name}`; ensure(`${dir}/internal`); ensure(`${dir}/tests`);
 write(`${dir}/README.md`, `# ${title(m.name)} Feature Capsule\n\n## Purpose\n${m.purpose}\n\n## Responsibilities\n- Maintain ${m.name} domain rules and state transitions.\n- Expose stable public contracts from \`public.ts\` and \`api.ts\`.\n- Keep UI adapters in \`ui.ts\` thin and replaceable.\n\n## Architecture\nThis capsule separates public contracts, state shape, UI adapters, and internal implementation notes. Existing runtime code may still live in \`src/\` during the incremental migration; this folder is the authoritative boundary for future work.\n\n## Public API\n${m.publicApi.map(x=>`- \`${x}\``).join('\n')}\n\n## Internal API\nInternal implementation belongs under \`internal/\` and must not be imported by other modules.\n\n## Dependencies\n${m.dependencies.map(x=>`- ${x}`).join('\n') || '- None'}\n\n## Events\n${m.events.map(x=>`- \`${x}\``).join('\n')}\n\n## State ownership\nOwns only state declared in \`state.ts\`; cross-module data should be referenced through public APIs or events.\n\n## Current limitations\n- Runtime extraction from \`src/App.tsx\` is intentionally incremental to avoid breaking behavior.\n- Tests are scaffolded and should be filled as code migrates.\n\n## Future roadmap\n- Move domain logic from \`src/\` into \`internal/\`.\n- Add capsule-level unit tests.\n- Document new events in \`docs/events.md\`.\n\n## Example usage\n\`\`\`ts\nimport * as ${m.name} from './public';\n\`\`\`\n\n## Directory structure\n- \`README.md\` — human overview.\n- \`AI.md\` — AI contribution rules.\n- \`TODO.md\` — local work queue.\n- \`CHANGELOG.md\` — capsule history.\n- \`TESTS.md\` — test strategy.\n- \`api.ts\` — facade exports.\n- \`state.ts\` — owned state types.\n- \`ui.ts\` — presentation adapters.\n- \`public.ts\` — stable external contract.\n- \`internal/\` — private implementation.\n- \`tests/\` — capsule tests.\n`);
 write(`${dir}/AI.md`, `# AI Context: ${title(m.name)}\n\n## Allowed responsibilities\n- Modify ${m.name}-owned docs, tests, state, and internal implementation.\n- Extend public interfaces only with backwards-compatible additions.\n\n## Forbidden responsibilities\n- Do not own behavior assigned to: ${m.dependencies.join(', ') || 'other modules'}.\n- Do not import from another capsule's \`internal/\` directory.\n- Do not change global app behavior without updating an ADR.\n\n## Coding style\n- Prefer small pure functions and explicit types.\n- Keep side effects at API boundaries.\n- Never wrap imports in try/catch blocks.\n\n## Architectural rules\n- \`public.ts\` is stable.\n- \`internal/\` is private.\n- Document new events in \`docs/events.md\`.\n\n## Naming conventions\n- Types use PascalCase.\n- Events use SCREAMING_SNAKE_CASE.\n- Functions use verb-first camelCase.\n\n## Public interfaces\n${m.publicApi.map(x=>`- \`${x}\``).join('\n')}\n\n## Internal interfaces\nInternal helpers should live in \`internal/\` and be re-exported only through \`api.ts\` when safe.\n\n## Common mistakes\n- Editing unrelated capsules to complete ${m.name} work.\n- Adding hidden cross-module coupling.\n- Forgetting TODO, TESTS, and CHANGELOG updates.\n\n## Files that should rarely change\n- \`public.ts\`\n- \`README.md\` architecture sections after stabilization\n\n## Preferred extension points\n- Add internals under \`internal/\`.\n- Add tests under \`tests/\`.\n- Add new public exports through \`api.ts\` after documenting them.\n`);
 write(`${dir}/TODO.md`, `# ${title(m.name)} TODO\n\n## Completed\n- Feature capsule scaffold created.\n\n## Current\n- Identify runtime logic in \`src/\` that belongs to this capsule.\n\n## Blocked\n- None.\n\n## Ideas\n- Add module-specific examples and fixtures.\n\n## Technical Debt\n- Runtime code has not yet been fully migrated from the legacy \`src/\` app shell.\n\n## Future Improvements\n- Add automated capsule test coverage and dependency checks.\n`);
 write(`${dir}/CHANGELOG.md`, `# ${title(m.name)} Changelog\n\n## 2026-07-19\n- Created AI-native feature capsule scaffold.\n`);
 write(`${dir}/TESTS.md`, `# ${title(m.name)} Test Strategy\n\n## Current coverage\n- Scaffold only.\n\n## Required tests\n- Public API contract tests.\n- State transition tests.\n- Event emission/consumption tests where applicable.\n\n## Test commands\n- \`npm run lint\` validates TypeScript contracts.\n`);
 write(`${dir}/OWNER.md`, `# ${title(m.name)} Ownership\n\n## Responsibilities\n${m.purpose}\n\n## Owned systems\n- State in \`state.ts\`.\n- Public surface in \`public.ts\` and \`api.ts\`.\n- Tests in \`tests/\`.\n\n## Dependencies\n${m.dependencies.map(x=>`- ${x}`).join('\n') || '- None'}\n\n## Public interfaces\n${m.publicApi.map(x=>`- \`${x}\``).join('\n')}\n\n## Non-responsibilities\n- Other gameplay capsules' private implementation.\n- Global application shell orchestration unless explicitly documented by ADR.\n\n## Future expansion\n- Move stable runtime logic from \`src/\` into this capsule behind public APIs.\n`);
 write(`${dir}/state.ts`, `export type ${title(m.name)}Status = 'planned' | 'in-progress' | 'blocked' | 'complete';\n\nexport type ${title(m.name)}State = {\n  module: '${m.name}';\n  status: ${title(m.name)}Status;\n  updatedAt: string | null;\n};\n\nexport const initial${title(m.name)}State: ${title(m.name)}State = {\n  module: '${m.name}',\n  status: '${m.status}',\n  updatedAt: null,\n};\n`);
 write(`${dir}/public.ts`, `export { initial${title(m.name)}State } from './state';\nexport type { ${title(m.name)}State, ${title(m.name)}Status } from './state';\n`);
 write(`${dir}/api.ts`, `export * from './public';\n`);
 write(`${dir}/ui.ts`, `export type ${title(m.name)}ViewModel = {\n  title: string;\n  status: string;\n};\n\nexport const create${title(m.name)}ViewModel = (status: string): ${title(m.name)}ViewModel => ({\n  title: '${title(m.name)}',\n  status,\n});\n`);
 write(`${dir}/internal/README.md`, `# ${title(m.name)} Internal\n\nPrivate implementation for the ${m.name} capsule. Do not import files from this folder outside \`gameplay/${m.name}\`.\n`);
 write(`${dir}/tests/README.md`, `# ${title(m.name)} Tests\n\nPlace capsule-level tests here. Prefer tests that exercise \`public.ts\` and avoid relying on private internals.\n`);
}
const rows = modules.map(m=>`| ${m.name} | ${m.status} | ${m.dependencies.length} | ${m.events.length} | ${m.publicApi.length} |`).join('\n');
write('docs/dashboard.md', `# Orchade Engineering Dashboard\n\nGenerated by \`npm run docs:generate\`.\n\n## Repository Health\n- Feature capsules: ${modules.length}\n- Documented capsules: ${modules.length}/${modules.length}\n- Dashboard status: scaffolded and ready for automation\n\n## Current Milestone\nAI-native repository architecture and incremental gameplay capsule migration.\n\n## Module Status\n| Module | Status | Dependencies | Events | Public API Entries |\n|---|---:|---:|---:|---:|\n${rows}\n\n## Completed Features\n- Capsule documentation standard.\n- ADR folder and initial decisions.\n- Graph placeholders.\n\n## Features In Progress\n${modules.filter(m=>m.status==='in-progress').map(m=>`- ${m.name}`).join('\n')}\n\n## Blocked Features\n- None recorded.\n\n## Build Status\nRun \`npm run build\` locally or in CI.\n\n## Broken Tests\n- None recorded by this generator.\n\n## Largest Modules\nTracked by future repository-health automation.\n\n## Most Modified Systems\nTracked by future repository-health automation.\n\n## Technical Debt\n- Runtime logic still lives mostly in legacy \`src/\`; migration should proceed capsule-by-capsule.\n\n## Documentation Coverage\n- Gameplay capsules: 100% scaffold coverage.\n\n## Testing Coverage\n- Capsule test folders exist; executable tests remain future work.\n\n## Recent AI Changes\n- 2026-07-19: Created AI-native engineering platform scaffolding.\n\n## Repository Statistics\n- Gameplay capsules: ${modules.length}\n- ADRs: 6 initial records\n- Graphs: 6 generated SVGs\n`);
write('docs/index.md', `# Documentation Index\n\n- [Engineering Dashboard](./dashboard.md)\n- [Repository Health](./repository-health.md)\n- [Repository Metrics](./metrics.md)\n- [Events](./events.md)\n- [ADR](./adr/)\n- [Graphs](./graphs/)\n\n## Gameplay Capsules\n${modules.map(m=>`- [${title(m.name)}](../gameplay/${m.name}/README.md)`).join('\n')}\n`);
write('docs/events.md', `# Event Catalog\n\nEvents are documented at capsule boundaries. Producers and consumers should use public APIs, never another capsule's internals.\n\n${modules.flatMap(m=>m.events.map(e=>`## ${e}\n- Producer: ${m.name}\n- Consumers: TBD by integration work.\n- Payload: Define in ${m.name}/public.ts before runtime use.\n- Lifecycle: Emitted after ${m.name} state changes are committed.\n- Failure conditions: Invalid payload, missing consumer, or rejected persistence.\n- Example: ${e} -> WORLD_UPDATED when global state must refresh.\n`)).join('\n')}\n\n## Example flow\nPLAYER_MOVED -> NPC_UPDATED -> WEATHER_UPDATED -> INVENTORY_CHANGED -> QUEST_PROGRESS -> SAVE_REQUESTED -> WORLD_UPDATED\n`);
write('docs/repository-health.md', `# Repository Health\n\nGenerated by \`npm run docs:generate\`.\n\n- Code duplication: measurement pending.\n- Unused assets: measurement pending.\n- Dead code: measurement pending.\n- Large files: measurement pending.\n- Dependency health: audited by CI via \`npm audit --audit-level=high\`.\n- Cyclomatic complexity: measurement pending.\n- Test coverage: capsule scaffolds present; executable coverage pending.\n- Documentation coverage: ${modules.length}/${modules.length} gameplay capsules documented.\n- Most active modules: pending git-history integration.\n- Least maintained modules: pending git-history integration.\n- Technical debt score: medium until runtime logic leaves legacy \`src/\`.\n- AI confidence score: medium-high for capsule discovery, medium for runtime isolation.\n`);
write('docs/metrics.md', `# Repository Metrics\n\nGenerated by \`npm run docs:generate\`.\n\n- Files: pending CI measurement.\n- Lines of code: pending CI measurement.\n- Commits: pending CI measurement.\n- Open Issues: requires GitHub API.\n- Closed Issues: requires GitHub API.\n- PR Velocity: requires GitHub API.\n- Average PR Size: requires GitHub API.\n- Largest Modules: pending repository-health integration.\n- Oldest TODOs: pending TODO timestamp convention.\n- Documentation %: gameplay capsule scaffold coverage is 100%.\n- Testing %: pending executable tests.\n- Build Time: captured in CI logs.\n- Dependency Count: see package lock audit output.\n`);
const svg=(name,label)=>`<svg xmlns="http://www.w3.org/2000/svg" width="900" height="260" role="img" aria-labelledby="title"><title id="title">${label}</title><rect width="100%" height="100%" fill="#0f172a"/><text x="32" y="48" fill="#e2e8f0" font-family="Arial" font-size="24">${label}</text><text x="32" y="92" fill="#94a3b8" font-family="Arial" font-size="16">Generated placeholder: replace with measured graph data as automation matures.</text><text x="32" y="140" fill="#38bdf8" font-family="Arial" font-size="15">${modules.map(m=>m.name).join(' → ')}</text></svg>\n`;
for (const [file,label] of [['module-dependencies.svg','Module Dependencies'],['event-flow.svg','Event Flow'],['state-flow.svg','State Flow'],['scene-flow.svg','Scene Flow'],['asset-pipeline.svg','Asset Pipeline'],['render-pipeline.svg','Render Pipeline']]) write(`docs/graphs/${file}`, svg(file,label));
