# Replay Guide

To reproduce a bug, load the same save snapshot, restore the same random seed state, and feed recorded commands/events through tick order. Compare checksums at checkpoint ticks to locate desyncs.
