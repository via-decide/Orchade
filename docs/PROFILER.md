# Profiler

Profiler primitives collect named metrics, frame profiles, memory profiles, and exportable reports.

## Metrics tracked

Simulation time, tick duration, FPS, entity counts, NPC counts, pathfinding cost, AI cost, weather cost, chunk loading, average update, worst update, and percentile summaries are represented through metrics and frame profiles.
