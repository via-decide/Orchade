# Performance Guide

Use scheduler timing for system-level costs and profiler metrics for runtime-level trends. Performance reports can be exported as JSON, CSV, or Markdown. Systems should declare budgets before large-scale NPC, pathfinding, or streaming work.
